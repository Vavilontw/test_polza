# Аномалии в `data_pack/review.csv`

## 1. Битая разметка полей — 1 строка

`c_001015`: `category='Пермь'`, `city='ул. Советская, д. 89, офис 43'`, `address=''` —
значения **сползли на одну колонку влево** (пропало значение category).
Обнаружено: `Пермь` оказалась единственным значением в `category`, которого нет в справочнике
категорий пачки, а в `city` попал текст, начинающийся с `ул.`.
Загрузчик такие строки пропускает (признак — пустой `address`), поэтому в базе id `c_001015` нет.

## 2. Двойное кодирование (mojibake) — 2 строки

`c_001008` (`РћРћРћ В«Р—Р°СЂСЏ РўРµС…В»` → `ООО «Заря Тех»`, `РњРѕСЃРєРІР°` → `Москва`) и
`c_001128` (`ООО «Темп Медиа»`, `Санкт-Петербург`).
Обнаружено: `s.encode('cp1251').decode('utf-8')` даёт осмысленный текст — значит utf-8 когда-то
прочитали как cp1251 и записали обратно. Файл в целом валидный utf-8, битые только эти две строки.
Загрузчик разворачивает round-trip (`fix_encoding`), в базе они лежат уже читаемыми.

## 3. Мусор в числовых полях

Пачка `page_*.json` чистая (типы правильные, `rating: null` где нет рейтинга) — в csv не так:

| id | поле | значение | что сделано |
|---|---|---|---|
| `c_001083` | rating | `N/A` | → NULL |
| `c_001122` | rating | `-3` | → NULL (вне 0..5) |
| `c_001186` | rating | `7.2` | → NULL (вне 0..5) |
| `c_001010` | rating | `4,5` | → 4.5 (десятичная запятая) |
| `c_001116` | reviews_count | `-10` | → 0 |
| `c_001187` | reviews_count | `много` | → 0 |
| `c_001079` | reviews_count | `45.5` | → 45 (дробное число отзывов) |

Обнаружено регуляркой по всем значениям колонок `rating`/`reviews_count`.
NULL/0 вместо мусора — то, что описано в комментариях `schema.sql`; проверки схемы
(`rating between 0 and 5`, `reviews_count >= 0`) без этой чистки отбили бы вставку целиком.

## 4. Города и категории: расхождения со справочником

Обнаружено вычитанием множества значений csv из множества значений пачки:

- `c_001182`: `Санкат-Петербург` — опечатка, в базу попала как отдельный город
- `c_001108`: `Moscow` — латиницей вместо `Москва`
- `c_001047`: `москва` — со строчной буквы
- `c_001184`: `Москва ` — с хвостовым пробелом (загрузчик обрезает `strip()`, слилась с `Москва`)

Первые три оставлены как есть: тащить таблицу синонимов ради трёх строк — оверинжиниринг,
но в отчёте по среднему рейтингу по городам они уже вылезают отдельными «городами» с одной
компанией и портят топ (`Санкат-Петербург` — 4.70 по одной компании).

## 5. Прочий мусор в текстовых полях

- `c_001064`: `site='нет сайта'` — текст вместо URL
- `c_001020`: `site='htp://sintez-service-453.ru'` — опечатка в схеме
- `c_001004`: `phone='8 (925) abc-12-34'` — буквы в номере, формат `8 (` вместо `+7 (`
- `c_001091`: `phone='+7'` — обрубленный номер

Обнаружено: `site` не начинается с `http`, `phone` не матчится на `+7 (XXX) XXX-XX-XX`
(в пачке этому формату соответствуют 100% непустых телефонов).
Не чистилось: схема эти поля не валидирует, а угадывать номер по огрызку нельзя.

## 6. Дубли строк

- 3 id продублированы полностью (`c_001049`, `c_001050`, `c_001075`) — строки идентичны,
  найдены `Counter` по id и по кортежу всей строки
- 2 последние строки файла — полностью пустые (`,,,,,,,,`), загрузчик их пропускает
- Итого: 207 строк данных → 205 с id → 202 уникальных id → 201 в базе (минус битый `c_001015`)
